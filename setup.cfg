[mutmut]
paths_to_mutate=backend
tests_dir=tests/unit
runner=bash -c "source $HOME/smartnest-venv/bin/activate && pytest -x --tb=no -q"

# Note: backend/api/* will show "no tests" because those are integration-only
# and can't run in mutmut's environment (closed stdout/stderr issues).
# This is expected and documented.
