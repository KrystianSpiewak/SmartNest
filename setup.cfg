[mutmut]
paths_to_mutate=backend
tests_dir=tests/unit
runner=bash scripts/mutmut_runner.sh

# Excluded paths are handled inside scripts/mutmut_runner.sh:
#   backend/api/         — integration-only tests, unit runner shows "no tests"
#   backend/tui/screens/ — ~2800 cosmetic rendering mutants (style=, border_style=,
#                          title_align=, Rich markup strings); no business logic
