# SmartNest Development Dependencies
# Extends base.txt — includes everything needed for dev workflow.
# Install: pip install -r requirements/dev.txt

-r base.txt

# Testing
pytest>=9.0.0
pytest-asyncio>=1.3.0
pytest-cov>=7.0.0
httpx>=0.28.0

# Linting & Formatting
ruff>=0.15.0

# Type Checking
mypy>=1.19.0

# Mutation Testing
# Windows: mutmut requires WSL2 due to Unix process management (fork, setproctitle)
mutmut>=3.4.0
